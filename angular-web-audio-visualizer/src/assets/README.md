# Audio assets

Place a `sample.mp3` file here for the default track, or use the file upload control in the app.

The blog post references `assets/sample.mp3`; any local MP3 file with that name will work.
